# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: picoloom

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - id: picoloom
    main: ./cmd/picoloom
    binary: picoloom
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.Version={{.Version}}
    # Ignore unsupported combinations
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - id: default
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - LICENSE

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
  groups:
    - title: Features
      regexp: '^feat(\(.+\))?:'
      order: 0
    - title: Bug Fixes
      regexp: '^fix(\(.+\))?:'
      order: 1
    - title: Refactoring
      regexp: '^refactor(\(.+\))?:'
      order: 2
    - title: Others
      order: 999

release:
  github:
    owner: alnah
    name: picoloom
  draft: false
  prerelease: auto
  name_template: "{{.ProjectName}} v{{.Version}}"

# ------------------------------------------------------------------------------
# Docker: DISABLED - requires Chromium, handled by separate workflow
# See .github/workflows/docker.yml
# ------------------------------------------------------------------------------
